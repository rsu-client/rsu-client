These files are just normal .txt files!
You can open them in any text editor like wordpad
and notepad! :)

These launchers requires a local install of perl with wxwidgets (dwimperl or citrus-perl), this method is faster at starting up than the
Perl-launchers approach, but comes at the cost of more disk space used
(hundreds of megabytes).